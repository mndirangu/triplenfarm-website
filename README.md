# Triple N EcoSmart Farm — Standalone Deployment

This build embeds CSS, JavaScript, and the logo directly into every HTML page. It does not depend on `/assets/styles.css` or `/assets/app.js` loading correctly.

Upload all files and folders to the root of the GitHub repository, replacing the previous version. The homepage file must be `index.html` at repository root.
